{
  "name": "yanke.world",
  "version": "1.0.0",
  "description": "My homepage",
  "source": "src/index.html",
  "browserslist": "> 2%, last 2 versions, not dead",
  "scripts": {
    "start": "rm -rf dist && parcel -p 3000",
    "build": "rm -rf dist && parcel build --no-cache && cp -r public/. dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.8.2",
    "@parcel/service-worker": "^2.8.2",
    "@parcel/transformer-webmanifest": "^2.8.2",
    "gh-pages": "^4.0.0",
    "parcel": "^2.8.2",
    "posthtml-inline-svg": "^0.2.0",
    "prettier": "^2.8.1"
  },
  "dependencies": {
    "daynight": "^3.3.0"
  }
}
